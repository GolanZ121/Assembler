הפרוייקט מוגש על ידי:
גולן זיו 215704784
נדב אדלר 215637497

הסבר על הקבצים:
קובץ ה assembler הוא קובץ ה main וממנו נשלחות הקריאות לפונקציות שמטפלות בקובץ שהתקבל ובסוף (אם הקובץ תקין) מוציאות קבצי פלט. 

קובץ ה preAssembler מכיל את הפונקציות המרכזיות אשר מטפלות בפרישת המקרואים וממנה נשלחות קריאות לפנוקציות העזר אשר עוזרות לטפל בקובץ שהתקבל. הפונקציות גם  מייצאות הקובץ החדש הפרוש.

קובץ ה first pass מכיל בין היתר את הפונקציה הראשית שמבצעת את המעבר הראשון וממנה נשלחות קריאות לפונקציות העזר אשר עוזרות לטפל בקובץ במעבר הראשון.

קובץ ה second pass מכיל את שלושת הפונקציות הראשיות של המעבר השני: הפונקציה שבודקת את שגיאות המעבר השני ומקודדת סמלים והפונקציות אשר יוצרות את קבצי ה ent, ext.
גם מה second pass נשלחות קריאות לפונקציות העזר.

קובץ ה utils הוא הקובץ המכיל את פונקציות העזר של המעבר הראשון והשני.

קובץ ה macroLists מכיל את פונקציות העזר של ה preAssemble.

קובץ ה convert מכיל את הפונקציה אשר ממירה את הקידוד הדצימלי ממערכי ההוראות והנתונים לקידוד בבסיס 64 ולבסוף יוצרת את קובץ ה object המתאים.

בנוסף מצורפים 5 קבצי טסט:
קובץ preAssemblerError שבוחן את השגיאות המטופלות בקדם אסמבלר ומוציא הודעות שגיאה.
קובץ firstPassError שבוחן את השגיאות המטופלות במעבר הראשון ומוציא הודעות שגיאה.
קובץ secondPassError שבוחן את השגיאות המטופלות במעבר השני ומוציא הודעות שגיאה.
קובץ warningsTest שבוחן את האזהרות המטופלות ולבסוף מוציא קבצי פלט.
קובץ validTest שהוא קובץ תקין בשפת אסמבלי ומוציא קבצי פלט.

כל קבצי הפלט נמצאים בתיקייה outputFiles.


 

