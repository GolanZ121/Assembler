/*this function gets the name of the file and creates an object file for him. it prints all all encoded data from the file to the object file. every encoded data is represented by 12 bits. it takes every six bits(right then left) and prints them in base 64 to the object file*/
void converter(char*, int);
